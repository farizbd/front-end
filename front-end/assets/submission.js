const firstName = prompt("Siapa nama depanmu?");
const lastName = prompt("Siapa nama belakangmu?");
 
const user = {
       first: firstName,
       last: lastName,
};

alert("hai.. " + user.first + " " + user.last + " mari bersama kita cegah corona.");